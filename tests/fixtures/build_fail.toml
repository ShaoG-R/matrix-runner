# This matrix contains a test case that is designed to fail during the build phase.
# 这个矩阵包含一个被设计为在构建阶段失败的测试用例。
language = "en"

[[cases]]
name = "build-failure-case"
features = "feature_build_fail"
no_default_features = false 